export type PeriodicElement = {
  number: number,
  name: string,
  weight: number, 
  symbol: string
}

export type PeriodicElements = PeriodicElement[];

export type FieldsData = {
  name: string,
  title: string
}